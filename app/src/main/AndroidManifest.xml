<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.messenger">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".base.MainApplication"
        android:allowBackup="false"
        android:icon="@drawable/ic_laucher_app"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_laucher_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.Messenger"
        android:usesCleartextTraffic="true">
        <activity android:name=".activites.search.SearchActivity"></activity>
        <activity android:name=".activites.gallery.GalleryActivity" />
        <activity android:name=".activites.sigin.SignInActivity" />
        <activity android:name=".activites.login.LoginActivity" />
        <activity android:name=".activites.send_message.SendMessageActivity" />
        <activity
            android:name=".activites.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>